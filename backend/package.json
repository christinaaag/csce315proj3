{
  "name": "backend",
  "version": "1.0.0",
  "description": "handles all calls to third party apis",
  "main": "index.js",
  "scripts": {
    "client": "cd ../frontend && npm run client",
    "server": "cd ../backend && node server.js",
    "start_all": "concurrently --kill-others \"npm run client\" \"npm run server\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "concurrently": "^5.3.0"
  }
}
